<% @page_title = 'Projects' %>

<div class="table-responsive">

  <table class="table">
    <tr>
      <th>Name</th>
    </tr>

    <% @projects.order('updated_at desc').each do |project| %>
      <tr>
        <td>
          <%= project_links(project) %>
        </td>
      </tr>
    <% end %>
  </table>
</div>
